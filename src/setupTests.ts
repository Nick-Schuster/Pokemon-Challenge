// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom

// This file configures the testing environment for Jest.
// It runs before each test suite and allows you to import global testing libraries
// like @testing-library/jest-dom for extended matchers or mock APIs for tests.
// Customize this file if you need to set up global test configurations.

import '@testing-library/jest-dom';
